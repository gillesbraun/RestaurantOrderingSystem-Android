<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/menu_clear_cache"
        android:title="Clear Cache"
        app:showAsAction="never"
        />
    <item
        android:id="@+id/menu_edit_order"
        android:visible="false"
        android:title="Edit Order"
        android:icon="@mipmap/ic_edit_white_24dp"
        app:showAsAction="always"
        />
    <item
        android:id="@+id/menu_change_waiter"
        android:visible="false"
        android:title="Change Waiter"
        android:icon="@mipmap/ic_people_white_24dp"
        app:showAsAction="always"
        />
    <item
        android:id="@+id/menu_change_location"
        android:visible="false"
        android:title="Change Location"
        android:icon="@mipmap/ic_edit_location_white_24dp"
        app:showAsAction="always"
        />
</menu>
